// Homepage for UGMRA / Search page (First screen)
import React from "react";
import TextInput from "../component/textinput";

const Home = () => {
    return (
        TextInput("searchBar", "Search...")
    )
}

export default Home;